1주차 복습

1일차 : 환경설정하기 

```markdown
## 설치와 실행 문서 연결

과정별 실행 방법은 각 과정 폴더의 `SETUP.md`에서 확인합니다.

공통 설치와 계정 준비는 아래 문서에서 확인합니다.

```

❓py -m pip install --upgrade pip

### requirements.txt 만드는 과정

처음에는 대부분 **검색**합니다.

가상환경에 필요하다 생각되는 걸 설치해보고 사용 후 필요없는 건 지우고 필요한 것만 남겨서 pip freeze  하면 자동생성됌

pip uninstall pandas

예를 들면 

- 웹 서버 만들기 → `FastAPI`, `Flask`, `Django`
- HTTP 요청 → `requests`
- 데이터 분석 → `pandas`
- AI → `torch`, `tensorflow`
- 이미지 처리 → `Pillow`
- 크롤링 → `beautifulsoup4`

경력이 쌓이면 자주 쓰는 패키지는 자연스럽게 외워집니다.

2일차 복습

api불러오기, ai에이전트연동 

초보자는 `input()`의 결과가 항상 문자열(str)이라는 점을 기억하면 좋습니다.

변수 이름 뒤에 : str, : int, : bool
타입 힌트라고 합니다.

message.lower()

message.upper()

message[-5:]

```python
# sys 모듈은 Python이 기본으로 제공하는 모듈입니다.
# sys.exit()를 사용하면 프로그램을 즉시 종료할 수 있습니다.
import sys
```

```python
# age = 20
# has_ticket = True

**# if age >= 18 and has_ticket:**
```

```python
# _ 는 위 case에 모두 해당하지 않을 때 실행되는 기본 처리입니다.
  # 다른 언어의 default와 비슷하게 생각하면 됩니다.
  case _:
      print("알 수 없는 메뉴입니다.")
```

!image.png

```python
for number in range(1, 6):
    # total += number는 total = total + number와 같은 뜻입니다.
    total += number

```

```python
# range(1, 6)은 1, 2, 3, 4, 5를 차례대로 만들어 줍니다.
# 끝 숫자 6은 포함되지 않는다는 점을 기억합니다.
```

# type()은 값이나 변수의 자료형을 확인하는 함수입니다.

# str은 문자열, int는 정수, bool은 참/거짓 값을 뜻합니다.

print(type(user_name))
print(type(user_age))
print(type(is_beginner))

# input()으로 받은 값이나 따옴표로 감싼 숫자는 문자열입니다.

# 문자열 "1000"은 숫자 1000과 다르기 때문에 바로 계산할 수 없습니다.

price_text = "1000"

# int()는 숫자 모양의 문자열을 정수로 변환합니다.

price = int(price_text)
print("가격 + 500 =", price + 500)